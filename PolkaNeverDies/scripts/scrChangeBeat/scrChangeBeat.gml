/// @func scrChangeBeat(measure, bpm)
/// @desc Changes the # of beats/measure and beats/minute
/// @param measure # of beats per measure
/// @param bpm beats per minute

var m = argument0;
var b = argument1;

scrChangeBeatSpeed(b);
scrChangeBeatMeasure(m);